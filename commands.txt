mkdir -- create directory
cd --change directory
ls -- list files/directories/folder
ls -a --show hidden files/all files
rmdir --remove directory
rm -rf dir-name  --if directory not empty

cp --copy files from one folders to another folders
cat  -- create/modify/dispaly files


git pull origin master ---copy remote to local 
git push -f origin master -- push to remote


local --
staging--adde changes,committed
remote--



reset 
local repo and stagging --before commiting and private
git reset logId

revert 
remote and after commiting, undo existing commit and public
git revert LogId

remove untracked files
git clean -n  (it will show files)
git clean -f (it will remove untracked files)
